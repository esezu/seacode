
; 主页面路由
GET / = VideoController->index
GET /index = VideoController->index

; 视频列表路由
GET /list = VideoController->list
GET /list/@category = VideoController->list

; 分类路由
GET /category/@id = VideoController->category
GET /categories = VideoController->categories

; 视频详情
GET /video/@id = VideoController->detail

; 搜索
GET /search = VideoController->search

; API接口
GET /api/videos = VideoController->apiList
GET /api/video = VideoController->apiDetail
GET /api/categories = VideoController->categories

; 404处理
* = VideoController->notFound
